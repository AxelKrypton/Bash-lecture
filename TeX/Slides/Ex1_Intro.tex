%-------------------------------%
%  Author: Alessandro Sciarra   %
%    Date: 02 Jun 2026          %
%-------------------------------%

%~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~%
\begin{frame}<0>[label=Ex1,fragile]{Some guidance about the session}
    \vspace{-2mm}
    \begin{itemize}
        \item You have roughly 60 minutes\\
              \begin{onlyenv}<1>
                  \then You should manage to spend time on each exercise\\
                  \then If you are done earlier, just chill \Remark{the week will be intense}
              \end{onlyenv}
              \begin{onlyenv}<2>
                  \then You won't manage to do everything! \Remark{That's not expected either!}
              \end{onlyenv}
        \item<only@1> If I was too quick, spend some time reviewing the slides \Remark{exercise 1}\\
                      \then \PB{In any case use the slides while solving exercises!}
        \item<only@1> \PQ{For the moment you can only tackle exercises 2 and 3}\\
                      \then These are pedagogical, do them!
        \item<only@2> \PQ{Now you can tackle all exercises}
        \item<only@2> \PB{Use the terminal to experiment, if needed}
        \item<only@2> Start picking one among 5, 6 or 7 as you wish or run the following in your terminal:
              \begin{lstlisting}[style=MyBash, aboveskip=3mm, numbers=none, xrightmargin=30mm]
                  $ echo $(( RANDOM % 3 + 5 )) # What happens?
              \end{lstlisting}
        \item \PS{All exercises can be solved exclusively with what we learnt so far}
        \item Note down your questions, doubts, curiosities, \ldots
        \item \alert{\textbf{Remember:} No AI of any sort for the moment!} \Remark{ideally also not Copilot autocompletion or equivalent}
    \end{itemize}
    \begin{varblock}{}[0.62\textwidth]{Come up with suggestions!}
        In the closing sessions we'll solve an exercise together.
        If you have any preference, feel free to speak up.
    \end{varblock}
\end{frame}
%~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~%
