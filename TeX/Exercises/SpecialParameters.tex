% Which is the difference between $*   "$*"   $@   "$@"   ->  https://unix.stackexchange.com/a/94200
% but use the following script, no for loop yet

#!/bin/bash
printf "%d args:" "$#"
printf " <%s>" "$@"
echo