% 1. Find max integer using ** operator for several n $(( 2**n ))
%    Why is it $(( 2**64 ))$ zero? Try to use 65,66,67,etc
% 2. Read bash manual and understand the difference between echo $(( 36#A )) echo $(( 36#A ))
% 3. printf "%x %o\n" $(( 16 )) $(( 8 ))